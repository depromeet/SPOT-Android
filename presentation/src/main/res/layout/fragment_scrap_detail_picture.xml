<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_background_white">

    <com.dpm.designsystem.SpotAppbar
        android:id="@+id/spotAppbar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:elevation="4dp"
        android:textColor="@color/color_foreground_white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:menuIcon="@drawable/ic_dots_horizontal"
        app:menuIconColor="@color/color_foreground_white"
        app:menuIconSize="24"
        app:navigationIcon="@drawable/ic_chevron_left_big"
        app:navigationIconColor="@color/color_foreground_white"
        app:navigationIconSize="24"
        app:titleTextAppearance="@style/TextAppearance.Spot.Label03" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/vpScrap"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>